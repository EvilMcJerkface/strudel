JPA Implementation of Strudel EPI
=========
This artifact provides an implementation of EPI using Java Persistence API so
that a benchmark developed on EPI can run on any RDBMSs as long as it supports JPA.
